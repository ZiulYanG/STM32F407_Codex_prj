# STM32F407 仓库协作规则

## 新对话接手流程

- 在分析、规划或修改工程前，必须完整读取仓库根目录的 `PROJECT_HANDOFF.md`。
- 随后读取其中与当前任务直接相关的架构、硬件、升级、调试或验证文档，不要仅依赖对话历史。
- 开始工作前检查当前 Git 分支、工作区状态和最近提交，区分用户已有改动与当前任务改动。
- 如果 `PROJECT_HANDOFF.md` 与源码、`.ioc`、原理图、硬件实测或 Git 记录冲突，先查明原因；以可验证证据为准，并同步修正文档。

## 工作节奏

- 按一个可独立验证的小步骤推进，不同时展开多个阶段。
- MCU 功能修改原则上依次完成：静态检查或构建、烧录、COM3 日志或 SWD/寄存器验证。
- 未完成相应验证的功能只能标为“已实现、待验证”，不能标为“已完成”。
- 不因修改 `.ioc` 自动假设生成源码已同步；CubeMX 重新生成后必须检查 Git diff 和关键配置。
- 保留用户工作区中与当前任务无关的改动，不覆盖或回退用户代码。

## 交接文件同步

- 每次取得实质性进展、确认重要决策、定位故障根因、完成硬件验证或改变下一步时，都必须在本次任务结束前更新 `PROJECT_HANDOFF.md`。
- 至少同步：最近日期、当前阶段、当前任务、完成状态、验证证据、遗留风险和下一项单一交付。
- 只记录能够帮助后续接手者继续工作的事实；不要粘贴冗长原始日志，也不要重复已经稳定记录在其他文档中的全部内容。
- 若本次仅回答问题且未改变项目状态，可以不修改交接文件。
- 发布版本时，同时更新 `README.md`、`CHANGELOG.md`、固件版本号、改动文件、验证结果和踩坑记录。
- 除非用户明确要求，不自行创建提交、标签或推送远端；需要提交时先确认交接文件已经同步。

## 当前项目关键约束

- MCU 为 STM32F407ZGT6；Bootloader 和 Application 是两套独立 FreeRTOS/CMake 工程。
- Application 起始地址和向量表为 `0x08040000`；Bootloader 占用内部 Flash 前 256 KB。
- 调试日志使用 USART1 PA9/PA10、115200 8N1；当前电脑对应 COM3，COM11 是 DAPLink 虚拟串口。
- 烧录调试器为 DAPLink CMSIS-DAP；常用 SWD 速率为 100 kHz。
- 外部 SPI Flash 原理图型号为 W25Q128；当前板卡实测JEDEC ID为`52 21 18`，固件按`NM25Q128EVB`兼容料处理并同时保留W25Q128支持。
- TinyUSB 与 USBX 使用两个独立学习工程；不得实现为同一固件中的运行时切换。
- 默认禁止固件降级；紧急回退必须使用独立的专用授权签名。
